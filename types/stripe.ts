/**
 * Stripe-related types - Subscription status, plan IDs, webhook payloads.
 * Align with Stripe product/price IDs and Prisma subscription model.
 */

// export type SubscriptionStatus = "active" | "canceled" | "past_due" | "trialing" | "unpaid";
// export type PlanId = string;
