/**
 * Shared TypeScript types - Use across app, server, and tRPC.
 * Prefer inferring from Prisma/Zod where possible; extend here for API shapes and UI props.
 */

// Example: export type { User, Task, Agent } from "@prisma/client";
// export type PlanId = "free" | "pro" | "enterprise";
