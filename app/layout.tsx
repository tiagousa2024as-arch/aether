/**
 * Root layout - Aether
 * Wraps all routes. Provides providers (Auth, tRPC) and global metadata.
 * Typography: Inter (see globals.css).
 */

import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Providers } from "./providers";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "AETHER — AI Operating Layer",
  description: "Orchestrate distributed intelligence agents. Execute work with natural language commands.",
  icons: {
    icon: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
