# Styles

- **Global**: `app/globals.css` — Tailwind directives and CSS variables (Shadcn theme).
- **Per-page/component**: Use Tailwind classes; add component-specific CSS in globals or CSS modules if needed.
- **Design tokens**: Prefer Tailwind theme extension in `tailwind.config.ts` and CSS variables in globals.css.
